/// Graph管理
/// Graph に 属性がある（名前、値）
/// Graph に 複数の Nodeがある
/// Node に名前と属性がある（名前、価）
/// Node に関係があり、その関係にも属性がある（名前、値）
pub mod graph {
    pub struct Graph;

    impl Graph {
        pub fn new() -> Self {
            todo!("Construct a new Graph struct.");
        }
    }
}
